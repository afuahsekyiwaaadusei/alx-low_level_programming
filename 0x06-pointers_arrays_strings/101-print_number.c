#include "main.h"

/**
 *print_number - a function that prints an integer
 *@n: integer to be printed
 */
void print_number(int n)
{

	int num = 0;

	int i = 0;

	while (num > 0)
	{
		
	}


	char str[] = (char) n;
	
	_putchar(str);
}
